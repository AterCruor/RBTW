# Backlog

- Evaluate richer formatting heuristic for book descriptions (prefer the source with more HTML structure, fallback to longer text).
- Add a "Missing ISBN" note when neither ISBN is provided so lookup limitations are clear.
